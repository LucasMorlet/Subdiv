#ifndef FACE_H
#define FACE_H

#include <GL/glew.h>
#include <GL/glu.h>
#include <GL/freeglut.h>
#include <vector>
/*
class Face
{
    private :
        std::vector <GLuint> sommets;
        std::vector <GLuint> voisins_directs;
        std::vector <GLuint> voisins_faces;
        unsigned int nb_voisins;

    public :
        // Constructeurs/destructeur
        Face( void );
        Face( GLuint a, GLuint b, GLuint c );
        Face( GLuint a, GLuint b, GLuint c, GLuint d );
        // Accesseurs
        GLuint getSommet ( int i ) const;
        unsigned int getNbSommets ( void ) const;
        void addVoisinsCatmull ( Face& f, GLuint a );
        void addVoisinsCatmull ( Face& f, GLuint a, GLuint b );
        void getPatch ( std::vector<GLuint> &vec ) const;

    public :
        static void calculAdjacence ( Face& a, Face& b );
};
*/
#endif // FACE_H

